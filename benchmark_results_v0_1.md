# 解码器基准 v0.1：第一次结果

运行文件：`decoder_benchmark.js`

## 结果

| 数据 | 最佳低复杂度解码器 | 描述长度 | 隐藏后缀平均误差 |
|---|---|---:|---:|
| 算术序列 | arithmetic | 2 | 0 |
| 周期序列 | periodic | 3 | 0 |
| 二次序列 | differences | 3 | 0 |
| 斐波那契序列 | recurrence | 4 | 0 |
| 随机序列 | 无稳定基线 | ∞ | — |

多项式解码器在所有有限前缀上都能构造拟合，因此是一个有用的过拟合对照。它在算术和二次序列上可以正确外推，但在周期序列和随机序列上的误差很大。由此得到三个初步结论：

1. 拟合已知信息不等于解码成功；
2. 描述长度和未见数据预测必须联合评价；
3. 随机数据需要允许解码器输出“不确定”或“未发现稳定结构”。

## 下一步

下一步把反馈从“选择最佳候选”推进到“解释失败并生成新候选”。第一版反馈只处理三种情况：

- 差分不恒定：尝试更高阶差分；
- 周期检测失败：尝试递推表示；
- 所有结构都失败：输出不确定性，而不是强行生成规律。

反馈原型已经实现于 `decoder_feedback.js`。它用“描述长度 + 预测误差惩罚”选择候选解码器，并设置不确定性阈值。当前结果为：

- 算术序列 → 接受 arithmetic；
- 周期序列 → 接受 periodic；
- 二次序列 → 接受 differences；
- 斐波那契序列 → 接受 recurrence；
- 随机序列 → 输出不确定。

现在反馈还会记录失败类型：差分表示不匹配、周期表示不匹配、递推表示不匹配，以及“拟合成功但外推失败（疑似过拟合）”。这使反馈从“哪个答案最好”推进到“当前解码器为什么失败”。

多切分验证和参数化候选生成已加入 `decoder_adaptive.js`。在训练前缀为 6、7、8 的三次切分上，算术、周期、二次和斐波那契序列均保持稳定识别；随机序列仍被判为不确定。周期序列在更短前缀上证据不足的现象也被保留下来，说明“稳定性”本身是可测量的反馈信号。

本轮又加入了“差分→递推”候选。对差分序列 `[1, 1, 2, 3, 5, ...]` 的累积序列，系统成功改变表示层后识别出递推结构，平均误差为 0。这是第一次出现由表示转换产生的新解码路径。

候选生成现在由初步诊断触发：递推失配时扩大递推参数搜索，出现过拟合时尝试差分递推。被接受的结果会生成解码证书，包含解码器名称、表示规则、描述长度、平均误差、稳定性和验证切分。这样每个结论都带有可复查的来源。

## v0.2 验证协议

`decoder_experiment.js` 将每条序列分为 16 项开发数据和 4 项最终留出数据。候选只在开发数据的三个不重叠验证切分 `[8, 10, 12]` 上选择；证书冻结后，`decoder_verifier.js` 独立重算模型、检查开发数据哈希、检查证书预测，并扫描最终留出数据的第一个反例。

结果如下：

| 数据 | 冻结的解码器 | 最终留出检查 |
|---|---|---|
| 算术序列 | arithmetic | 通过 |
| 周期序列 | periodic | 通过 |
| 二次序列 | differences | 通过 |
| 斐波那契序列 | recurrence | 通过 |
| 差分递推序列 | difference→recurrence | 通过 |
| 随机序列 | 无证书 | 保持不确定 |
| 对抗序列 | arithmetic | 第 17 项发现反例 |

`decoder_smoke_test.js` 还验证了篡改预测值、替换开发数据和对抗留出数据都会被识别。当前证书证明的是有限样本假设，不是无限序列定理；复杂度仍是用于排序的启发式代理值，不是严格的编码长度。

## v0.3 迁移实验

`decoder_transfer.js` 为每个结构准备两个参数不同的变体。解码器家族先在第一个变体上选择，再用目标变体的 8 项前缀拟合、预测后 4 项。结果是同结构变体的精确迁移率为 `1.000`。

跨结构精确率为 `0.300`，这不是简单的程序错误：算术序列本身也是二阶递推，某些有限前缀也可能被多个结构同时解释。它显示了解码器之间的包含关系和有限数据下的不可辨识性。迁移能力应报告为“同结构成功率”和“跨结构重叠率”，不能把单一赢家当作唯一真相。

## v0.4 关系与不变量

`graph_decoder.js` 将信息对象扩展为有限无向图。解码器通过度数、连通性和边数识别环、路径、星形图、完全图和正则图。图的顶点标签被视为表示选择；规范哈希枚举不超过 8 个顶点的标签置换，因此同一个环在重新编号后仍能通过证书验证。

`graph_experiment.js` 的结果是：原环和重新编号后的环均通过环证书；路径、星形图和加弦图均被识别为反例。这个实验把“等价关系和不变量”从序列预测扩展到了关系结构。

## v0.5 自动不变量

`graph_invariant_discovery.js` 加入了 WL-1 颜色细化：每个顶点先按度数着色，再反复用自身颜色和邻居颜色多重集更新颜色。这样不需要预先命名“环”或“路径”，也能产生可比较的关系签名。

重新编号的六边环保持相同签名。六边环和两个不相连的三角形也产生相同签名，但小图规范键不同；这构成了一个明确的碰撞反例，说明“同一不变量”不等于“同构”。系统现在会记录这种不可区分边界，而不是把签名当成完备证明。

## v0.6 统一协议

`decoder_protocol.js` 将数列和图结构统一为同一输出协议：`domain`、`decoder`、`representation`、`hypothesis`、`complexity`、`residual`、`verification` 和 `limits`。同一协议已在算术序列、周期序列、六边环及重新编号的六边环上运行；四个案例均得到零残差和独立验证。

统一协议使“解码器”成为跨领域可比较的对象。下一步可以在协议层加入共同的迁移评分和不确定性校准，再接入符号方程或真实数据。

## v0.7 迁移评分与不确定性

`decoder_evaluation.js` 按信息域和命题类型分别统计覆盖率、已提出命题的成功率、可靠性表和 Brier 分数。概率只从独立的 `calibration` 分区估计，`test` 与 `stress` 分区使用不同记录 id；未知证据或样本不足时返回“不知道概率”，不会伪造置信度。

在合成测试中，序列命题的测试覆盖率为 `1.000`、成功率为 `0.833`，图同构命题的覆盖率为 `1.000`、成功率为 `0.750`。在刻意提高后缀突变比例的压力分布中，序列成功率降至 `0.333`，说明校准概率会受到分布变化影响；系统将这个偏移报告出来，而不是把校准值当作普遍真理。两类测试的 Brier 分数都优于“每次都确信正确”的基线。

## v0.8 符号方程

`equation_decoder.js` 将整数线性方程规范化：消除整体公因子、统一符号，并把变量名变化作为可选表示变换。`equation_verifier.js` 独立检查规范形式和变量重命名。

例如 `6x-3y=9`、`12x-6y=18` 被识别为同一规范关系；把常数改为 `10` 会产生反例。统一协议现已覆盖数列、图和方程，方程案例的整体倍乘验证残差为 0，错误常数残差为 1。

## v0.9 方程组

`equation_system.js` 将整数系数方程组转换为精确有理数矩阵并执行 RREF。它能识别行变换等价、计算秩和自由变量，并把矛盾行标记为不一致。实验中，等价方程组得到相同 RREF；依赖方程组的秩下降；右端常数冲突被识别为不一致。

统一协议现已覆盖数列、图、单个方程和方程组。方程组模块仍限于小型线性系统，RREF 是代数规范代表，不包含具体应用语义。

## v0.10 可读消元结论

方程组解码器现在会把 RREF 翻译成可读结论：主元变量等于常数加自由变量的有理系数；同时标出 `unique`、`inconsistent` 和自由变量。独立验证器会重新计算 RREF 和这些结论，篡改结论会使证书失效。

示例方程组得到 `x=2`、`y=1`；依赖方程组得到 `x=5/2-y/2` 并标出 `y` 为自由变量；矛盾方程组直接返回不一致。这使“推理结果”与“验证证据”在同一证书中对应起来。

## v0.11 推理轨迹

方程组证书现在保存换行、行缩放和行消元的完整轨迹。验证器在原始输入上逐步重放轨迹，并将结果与 RREF 比较；篡改消元因子会使证书失效。对行等价但写法不同的输入，验证器仍比较最终 RREF，同时把原证书轨迹限定在其来源输入上。

## v0.12 解码器自动生成

`decoder_generator.js` 开始自动组合表示变换和基础解码器。当前搜索空间包含原表示、一阶差分、二阶差分，以及常数、算术、周期、差分和递推推理器。系统在多个切分上比较组合，并只冻结所有切分都精确通过的候选。

实验中，算术序列自动得到 `identity→arithmetic`，二次序列得到 `identity→differences`，累积斐波那契序列得到 `1阶差分→recurrence`；随机序列保持不确定。这是“根据残差改变表示，再重新推理”的第一个自动搜索版本。

## v0.13 组合证书与统一协议

自动生成器现在会冻结完整变换链，并保存变换后的开发数据、基础模型、哈希和预测。`decoder_generator_verifier.js` 独立重做变换、调用基础模型、还原预测，并能检查留出反例。统一协议新增 `generated_sequence` 类型；累积斐波那契样例通过 `1阶差分→recurrence` 证书验证。

## v0.14 变换链迁移

`decoder_generated_transfer.js` 将源序列上发现的变换链冻结，再应用到参数不同的目标序列。算术、二次和累积斐波那契三个序列族的同族精确迁移率均为 `1.000`；应用到无关序列时不会保持精确预测。这表明迁移的对象是“表示变换 + 推理规则”的组合，而不是源数据本身。

## v0.15 迁移失败诊断

`decoder_failure_analysis.js` 对冻结变换链的失败进行分层诊断：变换后无法拟合时报告 `representation_mismatch`；训练前缀可拟合但留出段偏离时报告 `extrapolation_failure`，并给出第一个反例位置；全部通过时报告 `passed`。

算术链在被改变的后缀上报告第 13 项外推失败，在无关序列上报告表示失配。这个诊断把“迁移失败”转化为可反馈的更新信号。

## v0.16 失败驱动搜索

`decoder_adaptive_search.js` 将诊断结果接入搜索循环：先尝试零阶表示，若没有稳定候选，再增加差分阶数，直到上限；每一轮保存候选、误差和失败原因。算术和二次序列分别在零阶搜索中结束，累积斐波那契序列在一阶差分搜索中结束，随机序列在四轮搜索后保持不确定。

统一协议的 `generated_sequence` 类型现在使用该搜索循环，并把搜索轮次写入结果，因而可以复查系统是如何从失败走向新表示的。

## v1.0 统一入口

`decoder_v1.js` 和 `README.md` 将当前模块整理为可调用入口。单个对象或 JSON 数组都可以通过 `domain` 路由到数列、自动生成数列、图、方程或方程组解码器，并获得统一字段和批量汇总。输入错误会被单独统计，未知对象保留为不确定。

## v1.1 多项式表达式

`polynomial_decoder.js` 将稀疏整数多项式规范化：合并同类项、移除零项、排序单项式，并消除整体系数公因子。整体倍乘和项顺序变化会得到同一规范项集；改变一个单项式系数会被验证器报告为反例。统一入口现已覆盖多项式表达式。

## v1.2 自动因式分解

`polynomial_factorization.js` 在单变量、次数不超过 8 的首一整数多项式上搜索整数根，并保留未分解的剩余因子。验证器重新展开因子并与源多项式规范项集比较。`(x-2)(x+1)` 被自动发现；`x²+1` 保留为不可约剩余。该模块明确把搜索范围写入适用边界。

## v1.3 多项式不变量

`polynomial_analysis.js` 自动提取变量支持、总次数和齐次性，计算偏导数，并在齐次多项式上检查欧拉恒等式 `Σxᵢ∂P/∂xᵢ=dP`。例如 `x²+2xy+y²` 被识别为二次齐次多项式且恒等式通过；非齐次表达式残差为 1。统一入口新增 `polynomial_analysis` 类型。

## v1.4 有限猜想生成

`conjecture_engine.js` 将解码结果转成带边界的有限样本猜想：数列猜想保存变换链和预测，图猜想保存不变量模型，多项式猜想保存齐次性和欧拉恒等式。每个猜想都附带独立验证函数；验证失败返回反例或不确定，不把候选直接称为定理。

## v1.5 守恒量发现

`dynamics_invariant.js` 在二维多项式映射和限定次数空间内枚举候选函数 `I(x,y)`，并用符号代数检查 `I(F(x,y))=I(x,y)`。旋转映射自动发现 `x²+y²`，剪切映射发现 `y` 等不变量，膨胀映射只保留有限候选。所有返回候选均经过独立恒等式验证。

守恒量候选还可以进入猜想层：`conjecture_engine.js` 将发现的多项式不变量封装为有限命题，并允许把同一命题迁移到另一个映射上检查。旋转映射的 `x²+y²` 在自身上通过，在剪切映射上产生反例；次数降到 1 时返回不确定。

本轮增加 `linear_nullspace` 方法：它把 `I(F)-I` 的系数收集成整数矩阵，直接求出给定次数空间的有理零空间，再规范化为整数基。旋转映射在次数 2 内得到唯一的非平凡基 `x²+y²`；该结果与枚举法的候选验证相互独立，减少了系数范围带来的遗漏。

## v1.6 动力系统证书闭环

`dynamics_invariant.js` 现在生成包含映射、搜索边界和候选列表的证书；`dynamics_invariant_verifier.js` 不信任摘要，而是重新执行有限搜索，逐项核对候选规则，重放 `I(F(x,y))=I(x,y)`，并在固定整数点上检查轨道残差。篡改映射、规则文本或候选列表都会被拒绝。统一协议的 `dynamics` 类型已接入该验证器，并返回搜索范围、轨道检查记录和残差。

旋转映射在次数不超过 2、系数范围不超过 1 时通过证书验证并发现 `x²+y²`；将搜索次数降到 1 后没有非平凡候选，协议保留为“不确定”。这一区分强调：有限枚举中的“未发现”只是当前解码器的边界。

参数化实验把这个结果推广到旋转缩放族：x²+y² 的保持条件为 a²+b²=1。证书验证器会重算参数条件；(a,b)=(0,1) 通过，而 (1,1) 被报告为反例。

本轮加入双层可读性字段：动力系统证书保存面向机器的解码器、基空间大小和候选维度摘要，同时输出面向人的规则文本。验证器会重算机器摘要，防止只翻译结果而隐藏内部篡改。

新增参数族共同不变量实验：旋转、反射和单位映射样本共享 x²+y²；在新旋转参数上验证通过，在缩放参数 (1,1) 上发现反例。

参数条件推导原型从旋转缩放族的残差样本恢复 -1+a²+b²=0，并在额外样本上逐点复核。该步骤展示了从共同不变量到参数方程的进一步解码。

参数条件模块现在强制参数留出集：6 个样本拟合条件，额外样本独立检查。旋转缩放实验的留出残差全为 0，且结果标记 generalizes=true。

高阶扰动压力测试通过：一个参数族在 6 个拟合点上表现为零残差，但第 7 个留出参数出现非零残差，系统正确标记 generalizes=false。

参数条件证书接口已加入：正常旋转缩放样本输出 verified_parameter_condition，高阶扰动样本输出 overfit_candidate；独立验证器可在新参数集上重算条件。

多状态点压力测试通过：旋转缩放条件在 (1,0)、(0,1)、(1,1) 三个状态点联合验证；参数条件与状态轨道两个层次同时通过才会标记为稳定。

统一报告模块已加入：动力系统验证结果、低搜索范围不确定结果和参数区域未知结果会被分开统计，并同时显示人类翻译与机器内部表示的覆盖情况。

第一版端到端验收已完成：8 个代表案例全部达到预期状态，包含规则序列通过、随机序列未知、动力系统守恒量通过、低搜索范围未知、参数条件通过/未知以及符号多项式验证。

单参数二次条件实验通过：从缩放族残差恢复 -1+c²=0，并在额外样本上验证。

参数条件实验报告已生成：正常旋转缩放族为 verified_parameter_condition，高阶扰动族为 overfit_candidate。

总体验收已扩展到参数条件：主协议 8 个案例和参数条件扩展均通过，正常族被接受，高阶扰动族被拒绝。

总体验收现同时记录资源剖面并执行独立审计；29 个测试全部通过。
