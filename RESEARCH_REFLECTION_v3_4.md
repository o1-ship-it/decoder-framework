# v3.4 研究反思

## 实质推进

v3.3 只证明了一张有限转移表可以具有反馈价值。v3.4 把这张表提升为运行区间—局部响应生成族：只要运行区间和响应都至少有两个，根查询先确定区间，再查询对应的局部位置，最坏深度恒为 2；固定设计则必须同时准备根查询和所有局部位置。精确搜索在所有声明范围内的有效配置上复现了这个关系。

这使项目从“存在一个手工案例”前进到“一个明确有限机制族内的可验证规律”。无响应变化的 4x1 控制没有优势，说明优势依赖于机制中的两层不确定性，而不是参数数量本身。

## 不能过度解释的部分

参数留出不是学习泛化：生成规则、候选语义和公式都由研究者预先定义，holdout 只阻止针对个别配置调整基准。当前候选最终仍被转成有限查询表，系统也没有从原始轨迹中发现运行区间或局部响应结构。

## 路线判断

下一步必须缩小这个差距，而不是再扩大参数网格。应将同一结构嵌入小型二维分段整数映射，让候选输出由映射规则计算，并冻结映射参数与查询域。若条件优势不能在该更贴近旗舰方向的表示中保持，v3.4 应被保留为协议族研究，而不应作为黑箱动力系统发现能力的证据。
