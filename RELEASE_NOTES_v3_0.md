# Decoder Framework v3.0

v3.0 将旗舰方向落实为黑箱离散动力系统结构发现。输入只包含二维整数状态转移；系统在声明的低次多项式空间内拟合映射，用未参与拟合的转移验证该映射，然后对冻结映射搜索不变量，并由独立验证器重放整个证书。

## 新能力

- `blackbox_dynamics` 统一协议域：轨迹到映射，再到不变量；
- 映射空间限制为二维、整数系数、次数 1 或 2；不变量搜索次数限制为 1 至 4；
- 训练数量不足、特征矩阵秩不足、训练数据不一致和非安全整数系数均返回不确定；
- 留出轨迹与推断映射冲突时返回 `counterexample_found`，不进入不变量搜索；
- 黑箱证书可独立重放，篡改映射或不变量会被拒绝；
- v3.0 基准包含旋转、非线性漂移和留出反例三个案例。

## 同时保留的负结果

有限坐标变换与原有多项式不变量搜索可以组合并验证，但在当前表示空间和测试映射上没有产生原坐标搜索无法得到的新不变量。因此它是可复现的消融对照，不作为 v3.0 的能力提升主张。

## 运行

`npm run benchmark:v3.0` 会生成 `benchmark_v3_0_results.json`。完整检验依次运行 `npm test`、`npm run acceptance`、`npm run audit` 和 `npm run manifest`。
