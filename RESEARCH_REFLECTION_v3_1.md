# v3.1 研究反思

## 得到的能力

系统现在能把“还不知道映射是什么”表达为一个显式有限版本空间，并提出能保证排除最多候选的下一个状态。这个建议不是对人类直觉的复述：每个候选状态都由预测分组、最大组大小、保证排除数和证书重放支撑。

## 必须接受的负结果

在当前的一次线性映射族中，主动策略确实优于完全随机的无重复采样，但没有胜过恰当选择的固定对角状态。这里的目标是恢复一个已知线性模型族的满秩设计，反馈不会增加信息论上必需的维度。把随机基线的优势写成主动学习优势是不成立的，因此没有这样宣称。

## 路线修正

下一阶段不应盲目扩大状态维数或系数范围。它应先定义能让反馈真正影响后续查询价值的任务，例如有限的竞争模型族、稀疏结构或带约束的可查询状态；同时必须与最优或接近最优的非自适应设计比较。若仍然没有稳定优势，项目应把主动模块定位为可解释的实验规划工具，而不是性能主张。
