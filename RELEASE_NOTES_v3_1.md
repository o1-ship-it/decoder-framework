# Decoder Framework v3.1

v3.1 将主动证据选择接入黑箱动力系统。给定已观测的二维状态转移，系统枚举声明的一次整数映射版本空间，按候选映射对每个未观测状态的预测分组，并选择能最大化最坏情况排除数的下一状态。结果、候选摘要和推荐状态均可由证书重放。

## 新能力

- `blackbox_active_observation_design` 协议域；
- 版本空间包含每个系数在 `-1..1` 的二维一次整数映射；
- 输出存活候选数、SHA-256 候选摘要、推荐状态、最坏剩余候选数、保证排除数和熵；
- 空版本空间返回 `counterexample_found`；唯一候选和无可区分状态分别保留独立状态；
- 完整性清单现在支持子目录路径，主动设计代码归入 `dynamics/`。

## 基准结果

v3.1 对系数为 `-1,0,1` 的全部 81 个线性部分和 8 个可选状态的所有 `8!` 个无重复被动采样顺序进行完整枚举。主动策略平均需要 1.950617 次新增观测；均匀被动顺序平均需要 2.114638 次，最坏需要 3 次。

这个结果不构成“主动学习优于强基线”的主张：一个固定的对角两点设计同样达到 1.950617 次。当前映射族已知且线性，适当固定点本身就能恢复满秩。该负结果是版本结论的一部分。

## 运行

运行 `npm run benchmark:v3.1` 生成完整比较结果。完整发布检验依次运行 `npm test`、`npm run acceptance`、`npm run audit` 和 `npm run manifest`。
