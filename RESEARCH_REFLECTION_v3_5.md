# v3.5 研究反思

## 实质推进

v3.4 的生成族仍把每个查询的转移表作为最终对象。v3.5 改为先定义二维分段整数映射，再对映射求值生成转移。这样，候选差异具有空间语义：原点区域确定运行区间，局部区域确定响应，背景区域不提供区分信息。查询设计模块无需知道这些语义，只验证由映射产生的输出。

在 33 个有效配置上，预先声明的深度关系与精确决策树一致；4x2、3x3 的映射参数留出保持严格优势，4x1 控制没有优势。这说明 v3.3 的反馈现象不是转移表序列化方式造成的，至少在该分段表示族中仍然存在。

## 仍未完成的部分

分段区域和候选参数仍由研究者直接给出。系统没有从原始状态转移中推断区域边界、响应参数或候选族；因此这不是黑箱动力系统发现的完整闭环。留出配置验证的是已知生成族的表示稳定性。

## 路线判断

下一步应把重点放在参数恢复：给定训练转移，在一个受限分段映射空间中生成全部一致候选，再用留出转移拒绝错误映射，最后才运行条件查询设计。只有这样，查询优势才真正连接到项目的“从不可读信息中寻找解码器”目标。
