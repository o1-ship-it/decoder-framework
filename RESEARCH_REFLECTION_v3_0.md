# v3.0 研究反思

## 本轮的实质推进

项目此前可以在已给定的显式动力系统上搜索不变量。v3.0 把输入后退一步：系统只获得状态转移。它必须先在预先声明的有限多项式空间中识别映射，再用独立留出轨迹淘汰错误映射，最后才调用符号不变量验证器。这个顺序把“解释观测”拆成了可审查的三个断点：表示空间、映射推断和结构验证。

非线性漂移基准说明，该过程不依赖旋转映射的名称或手写规则；但这仍只是对小型、无噪声、二维整数系统的受限函数空间结论。它不是未知黑箱的全局识别定理，更不是新的数学定理。

## 负对照的结论

本轮也实现了有限线性坐标变换后的组合不变量搜索。结果是负的：在当前的多项式表示和次数预算下，拉回后的候选已经可由原坐标多项式搜索找到。该模块证明了组合接口和证书重放可以工作，但没有提高发现能力。保留这项结果可以防止“增加一个解码器”被误写成研究进展。

## 路线判断

方向仍然正确，因为工作直接服务于北极星目标中的有限观测、可验证证书和反例边界；但系统还没有完成主动证据选择，也没有在隐藏生成分布上测量稳定的发现优势。下一阶段应集中在黑箱映射的主动观测设计：当训练数据秩不足或存在多个候选时，选择哪个新状态能最大幅度减少映射空间的歧义。先在明确的有限系数版本空间中定义该问题，再加入隐藏生成任务，避免再次扩张为彼此孤立的数学模块。
