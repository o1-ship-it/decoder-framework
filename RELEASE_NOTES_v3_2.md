# Decoder Framework v3.2.1

v3.2.1 修正 v3.2 的查询范围表述。证明只适用于 v3.1 实际使用的 8 个允许查询状态：`{-1,0,1}^2` 中除原点外的格点，而不适用于任意整数状态。对系数范围 `-1..1` 的二维整数线性映射，共有 81 个候选；枚举全部允许单次查询后，最多产生 25 个不同输出，仍不能区分全部候选。两个固定轴向查询直接恢复四个系数，因此该受限任务的非自适应最优查询数和自适应下界都为 2。

这不是主动学习性能提升，而是一个停止条件：继续在同一线性族扩大搜索不会产生反馈价值。下一阶段必须改变候选结构，并与最优固定设计比较。

运行 `npm run benchmark:v3.2` 可重放修正后的证明和篡改检测。
