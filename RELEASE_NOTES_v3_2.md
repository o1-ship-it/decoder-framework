# Decoder Framework v3.2

v3.2 将 v3.1 的主动观测负结果形式化为一个查询最优性证书。对系数范围 `-1..1` 的二维整数线性映射，共有 81 个候选。任意单次轴向查询最多产生 9 个不同输出，不能区分全部候选；两个固定轴向查询直接恢复四个系数。因此该任务的非自适应最优查询数和自适应下界都为 2。

这不是主动学习性能提升，而是一个停止条件：继续在同一线性族扩大搜索不会产生反馈价值。下一阶段必须改变候选结构，并与最优固定设计比较。

运行 `npm run benchmark:v3.2` 可重放证明和篡改检测。
