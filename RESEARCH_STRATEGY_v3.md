# v3 研究策略

## 中心问题

给定黑箱离散动力系统的有限观测，系统能否自动发现一个超出预置案例的结构，并把它转化为可验证、可区分、可迁移的数学命题？

## 阶段顺序

1. 统一候选、证书、验证器和反例接口；
2. 建立黑箱动力系统任务生成器，分离 calibration、test、stress 和 challenge；
3. 扩展组合解码器：表示变换 + 递推 / 不变量；
4. 对候选做观测等价商化和噪声下主动区分；
5. 将成功案例翻译为人类命题，并由符号验证器重放；
6. 在外部或隐藏任务上测量发现率、拒答率、迁移率和独立复核率。

## 研究停止条件

如果新增模块只增加案例数量，却没有提高隐藏任务发现率、候选区分效率、验证强度或迁移能力，就停止该方向并回到协议设计。

## 下一项具体工作

实现组合解码器的统一候选接口，并在离散动力系统中完成第一个“表示变换 + 不变量搜索”的隐藏 challenge。成功标准是：候选不由具体案例硬编码，能在独立生成任务上输出可重放证书，并报告失败边界。
