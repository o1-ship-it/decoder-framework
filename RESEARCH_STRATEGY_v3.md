# v3 研究策略

## 中心问题

给定黑箱离散动力系统的有限观测，系统能否自动发现一个超出预置案例的结构，并把它转化为可验证、可区分、可迁移的数学命题？

## 阶段顺序

1. 从黑箱轨迹推断受限映射，用留出数据验证并证书化不变量；
2. 在明确的有限候选空间中计算映射歧义和主动观测建议；
3. 建立与开发数据分离的生成任务，比较主动与被动采样的样本效率；
4. 对候选做观测等价商化，并在必要时加入有界噪声；
5. 将成功案例翻译为人类命题，并由符号验证器重放；
6. 在隐藏任务上报告发现率、拒答率、观测效率、迁移率和独立复核率。

## 研究停止条件

如果新增模块只增加案例数量，却没有提高隐藏任务发现率、候选区分效率、验证强度或迁移能力，就停止该方向并回到协议设计。

## 下一项具体工作

实现黑箱映射的主动观测设计。第一版只在声明的二维、低次、有限系数候选空间工作：当多个映射仍解释现有转移时，选择预测分歧最大的状态点，并输出候选分组、保证排除数和边界。成功标准是主动策略在分离的生成任务上用更少观测达到同等可识别性；若没有该优势，应停止扩展并复审候选空间或评价协议。
