# 研究反思与重新构想（v2.4）

## 本轮发现

完整有限候选空间显示，机制候选数和可观测预测类数不是同一个量。算术案例有 13 个通过当前数据的机制候选，但在未来 8 个观测的预测轨迹上只有 8 个等价类，压缩比为 8/13。参数化差异因此可能是机器内部差异，却不一定是当前解码器可读的信息。

## 路线判断

这一步与项目核心观点一致：可读性由表示和解码器决定。研究不应把内部模型数量直接当作信息数量，而应同时报告机制空间、预测空间和验证边界。

## 后续方向

下一阶段应将观测等价类扩展到组合解码器和高阶递推，并比较不同候选先验、不同 horizon 和不同噪声容差下的压缩率。只有当等价类在独立生成任务上稳定，才能讨论跨领域的信息度量。
