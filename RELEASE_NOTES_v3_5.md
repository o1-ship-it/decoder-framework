# Decoder Framework v3.5.0

v3.5 把 v3.4 的参数化黑箱候选从转移表提升为二维分段整数映射规则。候选映射在原点 `(0,0)` 返回运行区间，在匹配的局部条带 `(r,1)` 返回响应，在背景区域返回零；查询设计引擎只使用这些映射求值结果建立条件树和固定基线。

## 新能力

- `dynamics/piecewise_map_family.js` 生成、求值和验证二维分段映射候选；
- `blackbox_piecewise_map_query_design` 协议域输出映射参数、最坏查询深度、固定基线和证书摘要；
- 公式深度关系与完全查询搜索交叉验证；33 个有效配置全部一致；
- 证书重放覆盖映射规则、参数和精确查询分析，篡改会被拒绝。

## 基准结果

2x2、3x2 用作 calibration；4x2、3x3 和 4x1 用作 holdout。前两个 holdout 分别得到自适应 2 次、固定 5 次，以及自适应 2 次、固定 4 次；4x1 单响应控制为 1 次对 1 次。全部 33 个有效配置中，19 个严格优势、14 个无优势，公式与精确搜索 33/33 一致。

## 边界

分段区域语义、映射形式和查询域仍由研究者预先声明，候选限制为不超过 16 个、12 个查询的无噪声有限空间。该版本说明反馈优势能在映射表示中保留，不说明系统已经能够从未知轨迹自动发现分段规则。

运行 `npm run benchmark:v3.5` 可重放全部结果；完整发布检查还包括 `npm test`、`npm run acceptance`、`npm run audit` 和 `npm run manifest`。
