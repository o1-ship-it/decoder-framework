# 解码器理论 v0.1

## 目标

把“数学和理解是对信息进行分层解码”的观点，转化为可比较、可验证、可迭代的最小框架。

## 1. 基本对象

### 信息

信息是待解释的对象 `X`，可以是符号序列、数值数据、图结构、程序、实验记录或一组公理。

### 解码器

解码器定义为四元组：

```
D = (R, E, I, V)
```

- `R`：表示方式，例如坐标、变量、图、频率或程序；
- `E`：区分规则和等价关系，规定哪些差异重要；
- `I`：推理机制，用于生成结构、预测或行动；
- `V`：验证机制，用于检查一致性、预测误差和反例。

给定信息 `X`，解码器生成候选结构 `H`、预测 `X_hat` 和可执行操作 `a`：

```
D(X) -> (H, X_hat, a)
```

### 残差

残差 `E_D(X)` 是当前解码器无法解释、压缩、预测或验证的部分。残差不是单一数值，也可以包含反例、未定义情况和计算资源超限。

## 2. 可读性

信息没有脱离解码器的绝对可读性。给定资源预算 `B`，如果 `D` 能在预算内产生稳定、可验证且可复用的结构，则称 `X` 对 `D` 可读。

“不可读”分为四类：

1. 表达受限：当前表示方式无法表达；
2. 推理受限：规则不足以推出结果；
3. 计算受限：理论上可处理，但资源不足；
4. 认知受限：已有表示和规则尚未被理解或发现。

## 3. 解码器评价

解码器的综合质量定义为：

```
Q(D) = αC + βP + γT + δV - λK
```

- `C`：压缩能力，描述长度或模型复杂度越低越好；
- `P`：预测能力，在未见数据上的表现；
- `T`：迁移能力，在相关新任务上的表现；
- `V`：验证能力，能够被独立检查、证明或反驳；
- `K`：计算和表示成本。

具体权重应由实验任务确定，不能事先假定一种解码器永远最优。

## 4. 反馈和更新

反馈来自预测误差、反例、证明失败、迁移失败和新观察。解码器按下式更新：

```
D_(t+1) = U(D_t, feedback_t)
```

更新器 `U` 首先判断失败来自 `R`、`E`、`I` 还是 `V`，再修改相应组件。

## 5. 第一项实验

### 任务

给定有限前缀的符号或整数序列，寻找能够：

1. 压缩已知前缀；
2. 预测隐藏后缀；
3. 迁移到同类序列；
4. 说明自身适用条件；
5. 在随机序列上避免制造虚假规律。

### 基线解码器

- 多项式插值；
- 线性递推；
- 有限状态模式；
- 周期/频率表示；
- 简短程序表示。

### 关键对照

每个候选结构必须在未见数据上测试。随机序列作为负对照，防止把过拟合误判为发现规律。

## 6. 当前成功标准

第一版系统只有在以下条件同时满足时，才算有效：

- 在规则序列上获得稳定的压缩和预测；
- 在随机序列上保持较高复杂度或明确给出“不确定”；
- 能说明失败原因；
- 能根据反馈更换表示或推理规则；
- 输出可读的结构描述。

## 7. 可复查证书

被接受的解码器必须冻结以下信息：模型参数、表示规则、开发数据哈希、预测值、验证切分和适用边界。独立验证器重新执行模型，而不是信任解码器输出的摘要。

证书的声明限定为 `finite_sample_hypothesis`：它表示模型重现了给定有限数据，并通过了指定的有限验证。它不自动等价于关于无限序列的数学定理。

对于关系型信息，解码器还应区分表示标签和结构不变量。若图的顶点编号改变而不变量保持，解码器应将两者视为同一结构；若加入一条破坏不变量的边，验证器应返回反例。

不变量也有分辨率边界。颜色细化等算法产生的是结构签名；两个对象签名相同，只能说明当前解码器无法区分它们。系统必须保留碰撞样本，避免把“不可区分”误写成“等价”。

不同信息类型可以共享同一解码器协议。协议至少应返回对象域、表示规则、候选结构、复杂度、残差、验证状态和适用边界；具体的推理机制可以因信息类型而异。这样才能比较数列、图和方程解码器的迁移能力，而不要求它们使用相同的内部算法。

迁移评价应把命题类型和信息域分开统计。校准集只能用于估计条件成功率，测试集用于报告覆盖率、错误率和 Brier 分数；分布改变时要重新报告成功率，不能沿用原置信度。证据不足时，解码器应返回未知概率。

在线性方程中，表示变换可以是整体缩放、符号统一或变量重命名。规范化把这些变换对应的等价类映射到同一个代表元；验证器则检查输入是否仍属于该等价类。这个过程说明“解码”可以是将表达式压缩到不变量代表，而不只是在序列上做预测。

解码器生成可以建模为有限搜索：从表示变换集合和推理器集合中组合候选，再按独立验证结果筛选。组合候选的证书应记录变换链，避免只保存最终模型而丢失“如何改变表示”的信息。

组合证书应能被独立重放：验证器先重建原始表示的变换结果，再验证基础模型，最后执行逆变换得到预测。这样“新解码器”的可信度来自可复查的组成步骤，而不是来自生成器的内部状态。

迁移实验应冻结整条变换链，只改变目标对象及其参数。若同族对象仍能被精确解码，说明规则具有迁移性；若无关对象也被接受，则说明当前表示存在过宽的适用域，需要增加反例或更严格的验证。

迁移失败还应被分层记录：变换后无法拟合属于表示失配，训练可拟合而留出失败属于外推失配，训练和留出都通过才属于暂时确认。不同失败层级对应不同的更新动作，避免把所有错误都归因于参数。

失败诊断可以直接控制搜索：从低复杂度表示开始，只有在当前空间没有稳定候选时才增加变换阶数或更换推理器。每轮搜索都应保留，形成“残差—更新—新候选”的可审计反馈链。

当不同信息域共享入口协议时，解码器框架才成为可复用工具。统一入口不要求内部算法相同，只要求返回相同语义字段，并明确残差、验证范围和未知状态。

多项式规范化提供了符号表达式中的同一实例：同类项合并和整体系数约去把多个表面写法压缩为一个代表元；规范项集是可重算、可比较的解码结果。

因式分解展示了更深一层的解码：规范化之后，系统继续搜索能够压缩表达式的生成结构。若搜索范围内找不到因子，结果应保留剩余因子并注明边界，而不能把“未发现”写成“不可分解”。

猜想生成是解码器的输出层：系统把稳定结构转成一个明确命题，并同时给出证据范围、验证器和适用条件。猜想必须保留“有限样本”标签，直到获得更强的证明机制。

守恒量发现把解码对象推进到动力系统：在限定表示空间中搜索函数，再验证其在状态更新下是否保持不变。搜索空间之外的守恒量不能据此宣称不存在；“未发现”仍属于当前解码器的边界。

不变量分析是另一种解码层：从表达式中提取变量支持、次数和齐次性，并用偏导关系验证生成结构。欧拉恒等式为齐次多项式提供了独立可检验的关系，因此“理解”不只意味着找到更短写法，也意味着发现可复算的内部约束。

对于方程组，解码器可把行交换、非零倍乘和行加法视为表示变换，并把 RREF 作为规范代表。秩、自由变量和矛盾行是可验证的不变量；它们描述形式结构，不能单独决定具体应用中的语义。

在离散动力系统中，守恒量搜索提供了相同框架的动态版本。给定多项式映射 `F`，解码器可以在预先声明的次数空间中用线性代数求解 `I∘F-I=0`，也可以用有限系数枚举作为对照，并验证每个候选。证书验证器重新执行相同方法、重放符号恒等式，再用若干整数状态检查轨道残差。候选空间之外的函数仍属于不可读边界；没有候选时只能报告当前搜索范围内的不确定。

RREF 还可以生成分层结论：主元变量的精确表达式、自由变量集合以及一致性状态。结论必须由独立验证器重新计算；“可读”意味着表达式清晰，同时保留其由何种等价变换和验证规则得到的来源。

进一步地，推理过程本身也应成为可解码对象。交换行、缩放行和消去变量可编码为有限操作序列；独立验证器逐步重放该序列，因而验证的不只是最终答案，还有从输入到答案的路径。

## 8. 数据隔离协议

候选选择只使用开发数据。最终测试数据在证书冻结后才交给验证器；验证器报告“通过”或第一个反例的位置。开发数据哈希用于发现证书与原始数据之间的替换，模型重算用于发现预测摘要被篡改。

参数化解码器进一步把问题提升为‘哪些参数条件使守恒量成立’。例如旋转缩放族 F(a,b)(x,y)=(ax-by,bx+ay) 保持 x²+y² 当且仅当 a²+b²=1。该结论仍受限于声明的映射族，并由独立证书验证。

解码器还应区分内部可读性与交流可读性。机器可以使用人类难以直接阅读的零空间基、张量或程序表示；证书保存这些内部表示的摘要，同时提供可验证的人类翻译。内部表示可以不透明，但验证接口不能不透明。

更一般的参数化解码器可以把一组参数实例作为开发集，取各实例不变量空间的交集，形成共同结构候选，再在新参数上做独立验证。这相当于把迁移能力纳入参数条件发现。

参数条件推导可以视为另一层解码：先把状态变量上的守恒残差投影到参数空间，再寻找参数多项式关系。关系必须用未参与拟合的样本复核，避免把插值公式误当成一般定理。

参数条件解码遵循数据隔离：前 6 个参数样本只用于拟合二参数二次条件，后续样本只用于留出验证。若没有留出样本，系统不输出稳定条件。

压力测试是反馈机制的一部分：可以构造在开发参数上完全符合低阶条件、在留出参数上加入高阶扰动的族。若留出残差非零，系统必须把候选降级为过拟合，而不是保留为稳定规律。

参数条件的输出状态分为 verified_parameter_condition 和 overfit_candidate。只有留出集全部通过时才冻结为条件证书；候选形式即使在拟合集完全成立，也必须保留其过拟合状态。

参数条件不能只在一个状态点上验证。联合状态点检查把参数空间验证与状态空间验证分离，防止局部残差恰好为零造成伪条件。

统一评价报告只在同一信息域内汇总结果，分别记录验证率、反例率、未知率、残差、复杂度和机器/人类可读性。复杂度与可读性仍是代理指标，不应被压缩成跨领域单一排名。

端到端验收应同时包含成功、未知和反例案例。验收脚本只检查预先声明的有限命题是否得到预期状态，不把一次通过升级为无限定理。

参数条件求解可逐步推广：一参数一次模型作为最小通用单元，先验证参数残差的线性条件，再扩展到多参数和更高次数。

从一次条件推广到低阶多项式条件时，解码器能够表达离散根和对称参数区域；但次数和参数维度仍必须显式声明，并保留留出验证。

双参数一次条件是多参数解码的最小模型：用三个拟合样本确定系数，用额外参数样本验证关系是否泛化。

解码器评价应同时报告拟合残差、留出残差和可识别性。低拟合残差只能说明表达能力，留出残差与样本秩才决定条件是否具有可用证据。

不同参数条件模型应遵循同一数据隔离协议；即使一次条件只有两个自由系数，也不能使用全部样本反向调参后再宣称泛化。

实验脚本应保存参数条件的完整证据链：拟合样本、留出样本、残差和最终状态。这样后续可以比较不同解码器在同一参数族上的泛化边界。

评价层还需要独立审计：未知不能被计入成功，反例必须保留正残差，已验证结果必须具有零残差。审计器只检查分类一致性，不重新制造证据。

审计不仅检查命题状态，还应重算摘要统计。明细和汇总同时一致，报告才具有可复查性。

可复查解码器还需要版本清单：对核心源码、验证器和验收报告计算摘要，确保实验结论绑定到明确的内部表示和验证实现。

计算成本 K 也应进入证据记录。资源剖面记录运行时间和候选规模，但这些数值依赖硬件与实现，只用于同一环境下比较。
