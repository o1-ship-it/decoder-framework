# v3.6 研究反思

## 实质推进

v3.5 已证明二维分段表示能够保留反馈查询优势，但候选规则仍由研究者直接提供。v3.6 将观测接入候选生成过程：原始状态转移先筛选映射参数，独立留出再检验筛选结果，剩余歧义才进入查询设计。这样，参数恢复、反例反馈和主动证据选择形成了同一条可重放链路。

在冻结基准中，唯一参数可以被训练/留出组合恢复；只观测背景状态时，系统保留候选并给出主动计划；不符合映射族的训练或留出转移被明确报告为反例。4x2 的条件树深度为 2，固定基线为 5，说明反馈优势可以建立在观测筛选后的候选空间上。

## 仍未完成的部分

恢复过程枚举的是预先声明的区域语义和参数边界，尚未从轨迹自动发明分段结构。训练与留出均为无噪声精确整数转移，查询设计也受限于显式有限状态集合。当前结果证明的是“在给定表示族内解码参数并规划证据”的能力。

## 路线判断

项目已经从已知表示下的查询实验进入有限黑箱参数恢复，但距离开放式数学发现仍有明显鸿沟。下一步应优先扩大候选表示的可学习部分，同时保留可识别性和拒答输出：加入可枚举的区域边界候选或低阶分段模板，并用跨模板留出测试检验系统是否能区分“参数未知”和“表示族错误”。若扩展只增加候选数量而不提升隐藏任务识别率或证据效率，应停止该方向。
