# 研究反思与重新构想（v2.3）

## 复核发现

上一版主动设计只使用竞争器保留下来的模仿射赢家。复核完整参数空间后，发现这会低估候选数，并把区分所需的观测数报得过小。修正后的有限版本空间对 arithmetic、periodic、affine 和 modular_affine 全部枚举，并按精确预测序列保留候选。

## 新结果

在有界噪声容差 1 的算术案例中，当前留出数据后有 13 个候选；第 3 个新增观测把最坏区间重叠从 10 降到 5，保证排除 8 个候选。容差扩大到 20 时，保证排除数下降，说明信息损失可以被程序直接测量。

## 路线判断

方向仍然正确，但必须先保证版本空间完整，再谈信息增益或主动选择。后续研究应优先处理候选等价、噪声模型和独立验证器，而不是继续扩大规则数量。
