# Decoder Framework v3.3.0

v3.3 回答了 v3.1 与 v3.2.1 留下的关键问题：反馈式查询在强固定基线前是否可能有真实价值。答案是在一个完全明确、可穷举的有限黑箱族中为是，但该结论不自动推广到其他系统。

## 新能力

- `dynamics/branching_query_design.js` 精确搜索有限候选黑箱族的最优条件决策树和最小固定查询集合；
- `blackbox_branching_query_design` 协议域可运行默认分支族，也可接收同时给定的 `models` 与 `queries`；
- 证书重放会检查候选、查询、树和固定基线；篡改结论返回 `invalid_certificate`；
- 测试发现覆盖根目录和 `dynamics/` 中的测试文件，避免动力模块只在人工运行时才被检查。

## 基准结果

分支族包含 4 个候选黑箱和 3 个查询。`root` 将候选分为 A/B 两支；在 A 分支中只有 `branch_a` 有区分力，在 B 分支中只有 `branch_b` 有区分力。因此最优条件树最坏只需 2 次查询，而任意预先固定的查询集合必须包含全部 3 个查询。

基准还包含一个 4 候选、2 查询的无优势对照。在该对照中，条件树和固定设计都需要 2 次。这防止模块把“存在反馈优势”误报为“所有任务都有反馈优势”。

## 边界

结果只覆盖输入中显式给出的有限候选与查询，以及无噪声、最坏查询深度这个指标。实现将搜索限制为至多 16 个候选和 12 个查询。默认分支族是构造性存在证据，不是从真实动力系统自动发现的规律，也不是通用主动学习性能结论。

运行 `npm run benchmark:v3.3` 可重放两个基准案例；完整发布检查还包括 `npm test`、`npm run acceptance`、`npm run audit` 和 `npm run manifest`。
