# 从 v1 原型到前沿研究项目

## 必须跨过的三道门槛

### 1. 从演示到基准

建立公开、可生成、带隐藏测试集的 benchmark。每个任务记录表示空间、搜索预算、训练/验证/测试分区、反例类型和资源成本。评价指标包括发现率、留出正确率、拒答校准、证书重放率和迁移率。

### 2. 从候选到可复查数学

所有候选必须输出机器证书；独立验证器在干净进程中重放。对能翻译的结构输出人类命题，对暂时不可翻译的结构保留机器表示、预测行为和反例边界。有限样本结果继续标记为猜想。

### 3. 从规则搜索到机器原生解码

允许高维向量、程序和张量成为内部表示，但要求稳定性、迁移性和可验证性。研究重点不是让每个中间量都可视化，而是测量它是否携带可复用结构，以及能否提取部分可证明性质。

## v1.1 实施顺序

1. 将现有各域案例转换为统一 benchmark JSON schema；
2. 加入固定随机种子、隐藏留出集和资源预算；
3. 为每个解码器提供简单基线和消融实验；
4. 将证书验证接入持续集成；
5. 增加多参数、高阶、噪声和分布变化任务；
6. 记录机器内部表示与人类翻译之间的信息损失。

## 什么结果才算“前沿”

不是案例数量更多，而是在公开任务上证明新解码器相对于强基线具有可重复优势，同时能解释优势来自表示、推理、验证还是迁移，并公开失败样本和边界。

第一项 v1.1 实施已完成：公开 benchmark schema、固定 train/test 分区、基准运行器和结果汇总。当前基准包含 2 个已验证案例和 2 个未知边界案例。

### v1.3 当前进展
已完成通用多参数一次条件求解器：任意命名参数、秩不足拒答、拟合/留出残差和泛化状态。

### v1.6 当前进展
已完成隐藏生成机制发现原型：从序列前缀搜索模仿射一阶递推，在留出后缀上验证预测，并输出可独立重放的证书。对搜索范围内无法稳定识别的序列返回不确定状态。

### v1.7 研究问题
比较多个候选生成机制的证据强度，扩展到组合表示、多阶递推和分布变化压力测试；重点测量发现率、拒答校准、迁移率与证书重放率之间的关系。

### v1.7 当前进展
已完成多机制竞争原型：同时评估 arithmetic、periodic、affine 和 modular affine 候选；唯一候选、多个等价候选和无候选分别输出不同状态，并通过证书重放验证结果。

### v1.8 研究问题
在候选组合和高阶递推中测量模型选择偏差，建立 calibration 分区与 stress 分区隔离，报告拒答风险覆盖曲线和跨生成机制迁移率。

### v2.0 当前进展
已完成能力矩阵：将校准、测试和 stress 数据分开，统一输出发现、歧义、未知、覆盖率、选择性风险和 Brier 分数，并以任务摘要哈希和证书重放保证可复查。

### v2.1 研究问题
研究可识别性前沿：对通过当前证据的候选机制计算最早预测分歧，并把新增观测需求转化为主动实验设计指标。

### v2.1 当前进展
已完成可识别性前沿模块，能够区分唯一候选、有限 horizon 内可区分和 horizon 外仍不可区分三种状态。

### v2.2 研究问题
扩大可生成任务族，加入组合解码器和高阶递推，比较不同候选空间与不同实现之间的稳定性。

### v3.0 当前进展
已完成黑箱二维离散动力系统的最小发现闭环：从训练状态转移精确拟合次数受限的整数多项式映射，用留出转移验证或推翻映射，再搜索并重放多项式不变量证书。实现显式拒绝训练数量不足、秩不足和不一致数据；v3.0 基准包含一个非线性映射，以确认流程不依赖旋转案例。

### v3.1 研究问题
将主动设计移入黑箱动力系统主线。对当前证据下仍可能的有限系数映射版本空间，计算各候选状态的预测分组，选择保证减少映射歧义的状态，并用隐藏生成任务比较被动采样与主动采样的样本效率。只有当该比较显示可重复优势时，才扩展噪声或更高维状态。

### v3.1 当前进展
已完成有限映射版本空间上的主动状态选择和证书重放。完整枚举实验表明它优于均匀随机采样，却与适当固定对角设计平局。因此本版本没有把结果解释为主动学习的性能优势，而是将其定位为可审查的实验规划原型和一次明确的边界发现。

### v3.2 研究问题
寻找反馈改变查询价值的有限任务，而不是放大当前线性插值问题。每个候选任务必须先给出强固定设计基线，再评估主动策略的最坏、平均和样本效率；若优势不能超过该基线，应记录负结果并停止扩展。

### v3.2 当前进展
已将线性族的负结果形式化为可重放下界：在 v3.1 的 8 个允许格点上，单次查询的输出分辨率最多为 25，少于 81 个候选；两个固定轴向查询区分全部系数，因此自适应策略无法少于两次。该证明说明 v3.1 的平局来自受限查询空间的问题结构，而不是实现缺陷。

### v3.3 研究问题
转向具有反馈价值的有限竞争族或稀疏约束族，并把固定查询最优性作为先决基线。只有自适应决策树在该基线之下，才继续扩展主动观测。

### v3.3 当前进展
已完成一个精确有限分支族：第一次查询把候选分到 A/B 两支，后续查询的最优选择依赖该输出。完整枚举得到条件树最坏深度 2、固定查询最优深度 3；同时加入一个固定与自适应同为 2 的无优势对照。结论和篡改检测均可重放。

### v3.4 研究问题
将手工构造例升级为参数化且语义可解释的候选族，预先冻结训练/留出族配置，评估反馈优势是否能跨配置稳定复现。单个有限转移表只能证明优势可能存在，不能证明框架在未见问题上会自动找到它。
