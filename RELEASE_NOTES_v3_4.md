# Decoder Framework v3.4.0

v3.4 将 v3.3 的单一分支黑箱表推广为一个可解释的参数化族。每个候选黑箱由潜在运行区间和该区间的局部响应组成：根查询返回运行区间，只有对应的局部查询返回响应。这个结构使“第一次输出改变第二次查询的价值”成为明确的生成机制。

## 新能力

- `dynamics/parametric_branching_family.js` 从 `branchCount` 与 `variantCount` 生成候选黑箱和允许查询；
- 预先声明的深度关系与 `branching_query_design.js` 的完全穷举交叉验证；
- `blackbox_parametric_query_design` 协议域提供紧凑的参数、条件树深度、固定深度和证书摘要；
- 可重放证书会同时检查生成参数、公式和精确搜索结果；篡改任一结果会被拒绝。

## 冻结基准

有效配置限制为至多 8 个运行区间、8 个局部响应、16 个候选和 12 个查询。基准结果还记录所有 33 个有效配置的公式—穷举汇总：19 个严格优势、14 个无优势，且全部一致。基准将 2x2、3x2 参数配置列为 calibration；将 4x2、3x3 和 4x1 列为 holdout。前两个 holdout 的精确结果分别为自适应 2 次、固定 5 次，以及自适应 2 次、固定 4 次。4x1 的单响应控制为 1 次对 1 次，正确报告无优势。

“holdout”在这里仅表示参数配置不参与基准列表的校准选择。生成规则和深度公式本身不是从 calibration 数据拟合出来的，因此结果不是统计学习泛化分数。

## 边界

该版本建立了一个有限生成族中的稳定结构结论，但候选仍以有限查询表形式呈现。它不是从未知二维映射自动发现的规律，也不构成含噪、连续或更大规模系统的主动学习性能主张。

运行 `npm run benchmark:v3.4` 可重放校准、留出和无优势控制；完整发布检查还包括 `npm test`、`npm run acceptance`、`npm run audit` 和 `npm run manifest`。
