# v3.3 研究反思

## 得到的结论

v3.1 的线性映射实验与 v3.2.1 的下界证明说明：当固定查询已能恢复全部自由度时，反馈不会带来查询数优势。v3.3 改变的是任务结构，而不是调整启发式。第一次输出决定第二次应测试哪一个局部差异，使条件树可以复用第二层查询位置；固定设计必须同时准备两条分支所需的证据。

这个结果是项目主张的一个必要部分：框架现在既能用完整枚举证明“这里没有反馈价值”，也能用同样标准证明“这里有严格反馈价值”。两种结果都比只与随机采样比较更可靠。

## 仍未得到的能力

默认分支族是手工构造的有限转移表。引擎能验证、比较并重放它，却还不能从一类未见的真实动力系统中自行发现该结构，也没有证明优势会在噪声、连续状态或大候选空间中保持。把这一步称为数学发现或通用主动学习突破是不成立的。

## 路线判断

主线仍然正确：显式候选空间、独立证书、失败对照和强固定基线共同构成了可靠的研究闭环。下一步不该添加更多孤立示例，而应建立参数化且可解释的候选族，冻结部分族配置作为留出集，测量条件查询优势能否跨配置复现。若不能复现，v3.3 应作为边界清晰的存在性结果保留，而不是继续扩大功能面。
