# Decoder Framework v3.6.0

v3.6 将二维分段映射从已知规则下的查询设计推进到有限观测恢复：系统枚举声明映射族中的候选，用训练转移筛选参数，再用独立留出转移淘汰错误候选，最后在剩余候选上计算条件查询设计。

## 新能力

- `dynamics/piecewise_map_inference.js`：训练筛选、留出验证、参数恢复、歧义分类和主动查询计划；
- `blackbox_piecewise_map_inference` 协议域：输出恢复参数、剩余候选、失败阶段和证书摘要；
- v3.6 基准覆盖唯一恢复、部分恢复、主动优势、训练冲突和留出冲突，共 6 个冻结案例；
- 证书篡改检测覆盖完整分析结果，未知与反例不会被标记为已验证。

## 基准结果

6/6 案例通过。4x2 留出主动设计为最坏 2 次、固定基线 5 次；2x2 主动案例为 2 次对 3 次。唯一恢复案例正确得到区域 1、响应 1；训练和留出冲突分别在对应阶段拒绝。

## 边界

分段区域语义、映射形式、候选参数边界和查询状态仍由研究者预先声明；每个分区最多 12 条转移，候选最多 16 个。结果只对声明的无噪声二维整数映射族和有限观测成立，不宣称自动发现未知分段规则。

运行 `npm run benchmark:v3.6` 可重放基准；完整发布检查包括 `npm test`、`npm run acceptance`、`npm run audit` 和 `npm run manifest`。
