# Working Context

Read `PROJECT_CONTEXT.md` before exploring historical material. Read `CODEBASE_MAP.md` before changing code outside the current module.

The active research path is black-box discrete dynamics. New work must include an explicit finite search boundary, an independently replayable certificate or benchmark result, a failure case, and comparison with a relevant baseline.

Historical release notes and reflections are context on demand. Do not load them by default. Keep public protocol output compact; detailed evidence belongs in certificates and benchmark artifacts.
