const assert=require("node:assert/strict"),fs=require("node:fs"),runner=require("./benchmark_v1_4_runner.js");const r=runner.run(JSON.parse(fs.readFileSync("./benchmark_v1_4.json","utf8")));assert.equal(r.results.length,2);assert.ok(r.results.every(x=>x.verificationStrength==="verified"));assert.ok(r.results.every(x=>x.translationCoverage!==null));console.log("benchmark_v1_4_runner_test: passed");
