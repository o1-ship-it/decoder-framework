const fs = require("node:fs"); const path = require("node:path"); const protocol = require("./decoder_protocol.js");
function run(benchmark) { if (!benchmark || benchmark.schemaVersion !== 1 || !Array.isArray(benchmark.cases)) throw new TypeError("v2.3 benchmark 无效"); return { schemaVersion: 1, claim: "decoder_benchmark_run", benchmarkVersion: benchmark.version, seed: benchmark.seed, results: benchmark.cases.map(item => { const r = protocol.decode({ domain: item.domain, development: item.partitions.train, holdout: item.partitions.test, options: item.options }); return { id: item.id, expected: item.expected, actual: r.verification, recommendation: r.hypothesis }; }) }; }
function main() { const b = JSON.parse(fs.readFileSync(path.join(__dirname, "benchmark_v2_3.json"), "utf8")); const r = run(b); fs.writeFileSync(path.join(__dirname, "benchmark_v2_3_results.json"), JSON.stringify(r, null, 2), "utf8"); console.log(`v2.3 benchmark：${r.results.length} cases`); return r; }
if (require.main === module) main(); module.exports = { run };
