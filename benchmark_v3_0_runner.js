const fs=require("node:fs"),path=require("node:path"),protocol=require("./decoder_protocol.js");
function run(benchmark){if(!benchmark||benchmark.schemaVersion!==1||!Array.isArray(benchmark.cases))throw new TypeError("v3.0 benchmark 无效");const results=benchmark.cases.map(item=>{const r=protocol.decode({domain:item.domain,training:item.partitions.train,holdout:item.partitions.test,options:item.options});return {id:item.id,expected:item.expected,actual:r.verification,invariantCount:r.complexity};});return {schemaVersion:1,claim:"decoder_benchmark_run",benchmarkVersion:benchmark.version,seed:benchmark.seed,passed:results.every(item=>item.actual===item.expected),results};}
function main(){const b=JSON.parse(fs.readFileSync(path.join(__dirname,"benchmark_v3_0.json"),"utf8"));const r=run(b);fs.writeFileSync(path.join(__dirname,"benchmark_v3_0_results.json"),JSON.stringify(r,null,2),"utf8");console.log(`v3.0 benchmark：${r.results.length} cases，结果=${r.passed}`);return r;}if(require.main===module)main();module.exports={run};
