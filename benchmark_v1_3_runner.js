const fs = require("node:fs"); const path = require("node:path"); const protocol = require("./decoder_protocol.js"); const report = require("./decoder_report.js"); const schema = require("./benchmark_schema.js");
function run(benchmark) { schema.validateBenchmark(benchmark); return { schemaVersion: 1, claim: "decoder_benchmark_run", benchmarkVersion: benchmark.version, seed: benchmark.seed, results: benchmark.cases.map(testCase => { try { const start = process.hrtime.bigint(); const input = { domain: testCase.domain, family: testCase.family, samples: [...testCase.partitions.train, ...testCase.partitions.test] }; const result = protocol.decode(input); return { id: testCase.id, expected: testCase.expected, actual: result.verification, verificationStrength: report.verificationStrength(result), elapsedMs: Number(process.hrtime.bigint() - start) / 1e6 }; } catch (error) { return { id: testCase.id, expected: testCase.expected, actual: "error", verificationStrength: "unknown", error: error.message }; } }) }; }
function main() { const benchmark = JSON.parse(fs.readFileSync(path.join(__dirname, "benchmark_v1_3.json"), "utf8")); const result = run(benchmark); fs.writeFileSync(path.join(__dirname, "benchmark_v1_3_results.json"), JSON.stringify(result, null, 2), "utf8"); console.log(`v1.3 benchmark：${result.results.length} cases`); return result; }
if (require.main === module) main(); module.exports = { run };
