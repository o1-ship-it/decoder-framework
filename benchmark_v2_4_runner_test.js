const assert = require("node:assert/strict"); const fs = require("node:fs"); const runner = require("./benchmark_v2_4_runner.js"); const r = runner.run(JSON.parse(fs.readFileSync("./benchmark_v2_4.json", "utf8"))); assert.deepEqual(r.results.map(x => x.actual), ["observationally_distinguishable", "identified_hidden_structure"]); assert.ok(r.results[0].mechanismCount > r.results[0].predictiveClassCount); console.log("benchmark_v2_4_runner_test: passed");
