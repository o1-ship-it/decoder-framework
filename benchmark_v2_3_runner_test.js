const assert = require("node:assert/strict"); const fs = require("node:fs"); const runner = require("./benchmark_v2_3_runner.js"); const r = runner.run(JSON.parse(fs.readFileSync("./benchmark_v2_3.json", "utf8"))); assert.deepEqual(r.results.map(x => x.actual), ["noise_robust_disambiguation_plan", "noise_robust_disambiguation_plan"]); assert.ok(r.results[0].recommendation.guaranteedEliminations > r.results[1].recommendation.guaranteedEliminations); console.log("benchmark_v2_3_runner_test: passed");
