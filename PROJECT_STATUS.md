# 解码器理论项目状态

## 当前目标

把“数学和理解是分层信息解码”实现为可运行、可验证、可迁移的研究原型。

## 北极星目标

构建能够从有限观测中发现数学结构、输出独立可验证证书、表达不可识别性并主动选择下一条证据的解码器系统。工程系统是主体，形式理论是可靠性核心，数学难题是压力测试。旗舰方向是黑箱离散动力系统中的不变量与递推结构发现。

## 已完成能力

- 数列：算术、周期、差分、递推、组合表示搜索；
- 图：结构不变量、重新编号、WL-1 碰撞边界；
- 方程：规范化、方程组 RREF、可读消元结论和推理轨迹；
- 多项式：规范化、因式分解、齐次性和欧拉恒等式；
- 动力系统：守恒量枚举、线性零空间搜索、证书重放；
- 参数族：共同不变量、参数条件推导、留出集和高阶扰动压力测试；
- 评价层：迁移统计、未知状态、残差、机器 / 人类双层可读性和端到端验收。

## 核心约束

1. 有限样本验证不自动构成无限定理；
2. 候选结构必须有独立验证器；
3. 搜索边界外的“未发现”必须保留为不确定；
4. 机器内部表示可以不可读，但验证接口不能不可读；
5. 复杂度、可读性和成功率是任务相关指标，不能合并成虚假总分。

## 主要入口

- `node decoder_v1.js`：统一 JSON 解码入口；
- `node decoder_acceptance.js`：端到端验收；
- `node dynamics_condition_experiment.js`：参数条件实验；
- `node decoder_protocol_experiment.js`：跨领域协议实验。

## 下一阶段

推广到多参数高阶条件、带噪数据和真正机器原生的高维表示，同时保持证书、留出验证和反例反馈。

- 资源剖面：运行时间、状态和候选规模记录。
- v1.1 基准：固定 seed、train/test/stress 分区、资源预算、消融对照和持续集成。
- v1.2 起步：带噪数列鲁棒解码、容差和拒答 benchmark。
- v1.2 首个研究模块：鲁棒数列、噪声留出验证和 v1.2 benchmark。
- v1.2 扩展：stress 分布变化、翻译覆盖率和机器原生表示审计。
- v1.3：多参数一次条件、统一协议入口和 benchmark。
- v1.4：机器原生表示、翻译覆盖率、碰撞边界和 benchmark。
- v1.5 起步：机器原生解码器搜索和独立迁移验证。
- v1.6：隐藏生成机制发现；对模仿射序列进行有限参数搜索、留出验证和可重放证书；未知机制明确拒答。
- v1.7：多机制候选竞争、不可识别性标记、stress 残差和三类 benchmark。
- v2.0：能力矩阵、校准/测试隔离、选择性风险和可重放能力证书。
- v2.1：可识别性前沿、候选分歧位置和主动观测建议。
- v2.2：主动实验设计、候选切分熵和最小剩余歧义选择。
- v2.3：完整有限候选空间、噪声区间重叠和保守主动消歧。
- v2.4：观测等价类、机制候选压缩和完全识别 horizon。

总体验收已纳入资源剖面和独立审计。

- v1.1 benchmark：公开 schema、运行器和结果报告。

- v1.3 起步：通用多参数一次条件求解器，支持秩检查和留出泛化。

## v1.6 验收状态

- 总体验收：12/12 通过；
- 隐藏结构 benchmark：1 个验证案例、1 个未知边界案例；
- 验证方式：独立证书重放，篡改候选会被拒绝；
- 适用边界：当前仅搜索模数 2..32 的一阶模仿射递推。

## v1.7 计划

## v1.7 验收状态

- 总体验收：14/14 通过；
- 多机制 benchmark：唯一识别、歧义识别、未知拒答各 1 个案例；
- 歧义结果按 unknown 处理，不计入 verified 或 refuted。

## v2.0 验收状态

- 总体验收：15/15 通过；
- v2.0 benchmark：校准 2 个任务、独立测试 2 个任务，标签准确率 100%；
- 结果同时报告覆盖率、选择性风险和 Brier 分数。

## v2.1 验收状态

- 总体验收：16/16 通过；
- 可识别性 benchmark：歧义前沿与唯一结构各 1 个案例；
- 结果可通过证书重放，有限 horizon 内无法区分时明确保留歧义。

## v2.2 计划

扩展到组合解码器和多阶递推，引入更多独立生成任务和跨实现复核，同时保留机器证书、人类翻译和有限样本边界声明。

## v2.2 验收状态

- 总体验收：17/17 通过；
- 主动设计 benchmark：歧义规划与已识别各 1 个案例；
- 歧义案例推荐新增 1 个观测，并给出候选分组熵。

## v2.3 计划

研究组合解码器和多阶递推，并比较不同候选先验对实验选择的影响。

## v2.3 验收状态

- 总体验收：18/18 通过；
- 噪声 benchmark：容差 1 与容差 20 各 1 个案例；
- 容差增大时保证排除数下降，结果明确反映信息损失。
- 候选空间由显式有限版本空间完整枚举，避免隐藏丢弃同样符合数据的机制。

## v2.4 计划

研究组合解码器和多阶递推，并比较不同候选先验对实验选择的影响。

## v2.4 验收状态

- 总体验收：19/19 通过；
- 观测等价 benchmark：候选压缩与唯一结构各 1 个案例；
- 算术案例中机制候选多于预测类，证明参数歧义与观测歧义不同。

## v3.0 验收状态

- 总体验收：21/21 通过；
- 黑箱动力系统：从训练转移推断受限多项式映射，在独立留出转移上验证后搜索不变量；
- 基准：旋转、非线性漂移各 1 个验证案例，另有 1 个留出反例案例；
- 失败边界：训练样本不足、秩不足、训练不一致或留出冲突都会保留不确定或反例状态；
- 证书：篡改推断映射或不变量会被独立重放拒绝。

## v3.1 验收状态

- 总体验收：23/23 通过；
- 主动观测：显式枚举 81 个一次整数映射候选，按预测分组选择保证排除数最大的下一状态；
- 基准：完整枚举 81 个目标与每个目标的全部 8! 个被动无重复采样顺序；
- 结果：主动策略平均 1.950617 次新增观测，随机被动顺序平均 2.114638 次、最坏 3 次；
- 负结果：固定对角两点设计达到同样平均 1.950617 次，尚无相对强非自适应设计的主动优势。

## v3.2 计划

先构造存在真实反馈价值的有限任务族，并把最优或接近最优的非自适应设计作为基线。候选方向是竞争模型族、稀疏结构或受约束查询状态。只有主动策略稳定减少所需观测，才扩展到噪声、更高次数或更高维状态。

## v3.2 当前进展

- 已形式化当前线性映射族的负结果：在 v3.1 的 8 个允许非零格点中，单次查询最多产生 25 个不同输出，无法区分 81 个候选；两个轴向查询可唯一恢复四个系数，因此该受限查询空间内固定设计和自适应策略的查询下界都是 2；
- 已加入独立重放证明和篡改检测，作为主动设计继续扩展前的停止条件。

## v3.3 验收状态

- 总体验收：24/24 通过；
- 构造性分支族：4 个明确候选黑箱、3 个明确查询。最优条件决策树的最坏深度为 2，而任意固定查询集合需要 3 次；
- 无优势对照：4 个候选、2 个查询中，固定与自适应的最坏深度均为 2。系统不会把所有有限族都报告为反馈优势；
- 两类结果都由独立证书重放；篡改固定深度会被拒绝；
- 边界：这是对至多 16 个候选、12 个查询的显式有限转移表完全枚举，不是现实动力系统、连续状态、噪声或通用主动学习结论。

## v3.4 验收状态

- 总体验收：26/26 通过；
- 参数化族：候选黑箱由有限运行区间和局部响应构成。根查询识别运行区间，匹配的局部查询识别响应；
- 公式与精确穷举：在全部 33 个声明范围内的有效配置上，预先声明的深度关系与精确条件树、最优固定集合一致；
- 冻结配置：2x2、3x2 用作 calibration；4x2、3x3 作为 holdout 分别得到 2 对 5、2 对 4；4x1 holdout 控制为 1 对 1；
- 边界：这是生成规则已知时的参数留出验证，不是从数据学习该规则，也不代表对真实未知动力系统的泛化。

## v3.5 验收状态

- 总体验收：28/28 通过；
- 候选由二维分段整数映射规则求值生成：原点区域返回运行区间，匹配的局部区域返回响应，其余状态返回背景值；
- 33 个有效区域/响应配置全部公式—穷举一致（19 个严格优势、14 个无优势）；
- 映射参数留出：4x2、3x3 分别为自适应 2 对固定 5、2 对 4；4x1 无优势控制为 1 对 1；
- 边界：映射边界和查询域预先声明，候选仍限制在至多 16 个、12 个查询的无噪声有限空间；不是从真实轨迹自动发现分段规则。

## v3.6 计划

让候选映射由少量二维分段规则参数自动生成，并从状态转移训练数据恢复候选参数；将参数恢复、留出验证和查询设计连接起来。若仍需手写区域语义，结果应保留为表示层研究，不宣称完成黑箱动力系统发现。
