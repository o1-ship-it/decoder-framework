# 解码器理论项目状态

## 当前目标

把“数学和理解是分层信息解码”实现为可运行、可验证、可迁移的研究原型。

## 已完成能力

- 数列：算术、周期、差分、递推、组合表示搜索；
- 图：结构不变量、重新编号、WL-1 碰撞边界；
- 方程：规范化、方程组 RREF、可读消元结论和推理轨迹；
- 多项式：规范化、因式分解、齐次性和欧拉恒等式；
- 动力系统：守恒量枚举、线性零空间搜索、证书重放；
- 参数族：共同不变量、参数条件推导、留出集和高阶扰动压力测试；
- 评价层：迁移统计、未知状态、残差、机器 / 人类双层可读性和端到端验收。

## 核心约束

1. 有限样本验证不自动构成无限定理；
2. 候选结构必须有独立验证器；
3. 搜索边界外的“未发现”必须保留为不确定；
4. 机器内部表示可以不可读，但验证接口不能不可读；
5. 复杂度、可读性和成功率是任务相关指标，不能合并成虚假总分。

## 主要入口

- `node decoder_v1.js`：统一 JSON 解码入口；
- `node decoder_acceptance.js`：端到端验收；
- `node dynamics_condition_experiment.js`：参数条件实验；
- `node decoder_protocol_experiment.js`：跨领域协议实验。

## 下一阶段

推广到多参数高阶条件、带噪数据和真正机器原生的高维表示，同时保持证书、留出验证和反例反馈。

- 资源剖面：运行时间、状态和候选规模记录。
- v1.1 基准：固定 seed、train/test/stress 分区、资源预算、消融对照和持续集成。
- v1.2 起步：带噪数列鲁棒解码、容差和拒答 benchmark。
- v1.2 首个研究模块：鲁棒数列、噪声留出验证和 v1.2 benchmark。
- v1.2 扩展：stress 分布变化、翻译覆盖率和机器原生表示审计。
- v1.3：多参数一次条件、统一协议入口和 benchmark。
- v1.4：机器原生表示、翻译覆盖率、碰撞边界和 benchmark。
- v1.5 起步：机器原生解码器搜索和独立迁移验证。
- v1.6：隐藏生成机制发现；对模仿射序列进行有限参数搜索、留出验证和可重放证书；未知机制明确拒答。
- v1.7：多机制候选竞争、不可识别性标记、stress 残差和三类 benchmark。
- v2.0：能力矩阵、校准/测试隔离、选择性风险和可重放能力证书。
- v2.1：可识别性前沿、候选分歧位置和主动观测建议。
- v2.2：主动实验设计、候选切分熵和最小剩余歧义选择。
- v2.3：完整有限候选空间、噪声区间重叠和保守主动消歧。

总体验收已纳入资源剖面和独立审计。

- v1.1 benchmark：公开 schema、运行器和结果报告。

- v1.3 起步：通用多参数一次条件求解器，支持秩检查和留出泛化。

## v1.6 验收状态

- 总体验收：12/12 通过；
- 隐藏结构 benchmark：1 个验证案例、1 个未知边界案例；
- 验证方式：独立证书重放，篡改候选会被拒绝；
- 适用边界：当前仅搜索模数 2..32 的一阶模仿射递推。

## v1.7 计划

## v1.7 验收状态

- 总体验收：14/14 通过；
- 多机制 benchmark：唯一识别、歧义识别、未知拒答各 1 个案例；
- 歧义结果按 unknown 处理，不计入 verified 或 refuted。

## v2.0 验收状态

- 总体验收：15/15 通过；
- v2.0 benchmark：校准 2 个任务、独立测试 2 个任务，标签准确率 100%；
- 结果同时报告覆盖率、选择性风险和 Brier 分数。

## v2.1 验收状态

- 总体验收：16/16 通过；
- 可识别性 benchmark：歧义前沿与唯一结构各 1 个案例；
- 结果可通过证书重放，有限 horizon 内无法区分时明确保留歧义。

## v2.2 计划

扩展到组合解码器和多阶递推，引入更多独立生成任务和跨实现复核，同时保留机器证书、人类翻译和有限样本边界声明。

## v2.2 验收状态

- 总体验收：17/17 通过；
- 主动设计 benchmark：歧义规划与已识别各 1 个案例；
- 歧义案例推荐新增 1 个观测，并给出候选分组熵。

## v2.3 计划

研究组合解码器和多阶递推，并比较不同候选先验对实验选择的影响。

## v2.3 验收状态

- 总体验收：18/18 通过；
- 噪声 benchmark：容差 1 与容差 20 各 1 个案例；
- 容差增大时保证排除数下降，结果明确反映信息损失。
- 候选空间由显式有限版本空间完整枚举，避免隐藏丢弃同样符合数据的机制。

## v2.4 计划

研究组合解码器和多阶递推，并比较不同候选先验对实验选择的影响。
