# 项目北极星目标

## 一句话目标

构建一个能够从有限观测中发现数学结构、输出独立可验证证书、明确表达不可识别性，并主动选择下一条最有价值证据的解码器系统。

## 三个层级

### 1. 主体：工程可行的解码器发现系统

系统的输入是观测、表示空间和资源边界；输出不是强行拟合的公式，而是以下四类之一：

- 可验证的候选结构；
- 多个观测等价或尚未区分的候选；
- 没有在声明搜索空间内找到候选；
- 建议下一次观测或反例搜索。

系统必须支持候选生成、独立验证、证书重放、失败分析和主动实验设计。

### 2. 核心：可识别性与可验证性的形式理论

理论工作回答四个问题：

1. 哪些候选在当前证据下不可区分；
2. 哪些参数差异只是内部表示差异；
3. 多少新证据足以区分候选；
4. 什么条件下有限验证可以升级为指定函数空间内的证明。

复杂度、可读性、预测能力和证据强度必须分开报告，不能合成一个没有语义的总分。

### 3. 终极压力测试：数学问题

数学难题不是第三条并列产品线，而是验证解码器是否真正有用的压力测试。系统只有在公开或隐藏任务上发现人类没有预先写入的结构，并由独立验证器确认时，才算产生数学研究价值。

## 旗舰研究方向

后续不再平均扩展所有数学域，集中研究**黑箱离散动力系统中的不变量与递推结构发现**：

- 从有限轨迹推断候选不变量或递推；
- 在符号层面重放并验证候选；
- 对参数族推导适用条件；
- 计算观测等价类和反例边界；
- 主动选择状态点或额外轨迹以区分候选。

数列、图和多项式模块保留为基线、消融和迁移测试，不再作为彼此孤立的扩展方向。

## 何时算成功

一个版本只有同时满足以下条件，才算对总目标有实质推进：

1. 候选空间是明确且可复现的；
2. 结果有独立验证器和篡改检测；
3. 有隐藏测试或分布变化测试；
4. 系统能区分唯一结构、观测等价和未知；
5. 至少一个非平凡任务产生代码未直接写入的可验证结构；
6. 结果能告诉研究者下一步应获取什么证据。

当前项目已经满足前四项的原型要求，后两项是下一阶段的主要研究目标。

## 明确不追求的目标

- 不把有限样本拟合包装成无条件数学定理；
- 不把增加解码器数量当作研究进展；
- 不把机器内部向量的可视化当作解释本身；
- 不在没有独立验证和隐藏测试的情况下声称解决通用数学难题。
